# Server Configuration
PORT=3000
NODE_ENV=development

# API Key (for internal services)
API_KEY=local_dev_api_key_change_this

# Database Configuration
# LOCAL DEVELOPMENT: Use Docker Compose (run: docker-compose up -d)
DATABASE_URL=postgresql://postgres:password@localhost:5432/bloombuhay?schema=public

# JWT Configuration
# Generate a secure random key using one of these commands:
# Linux/Mac: openssl rand -base64 32
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your_very_secure_random_secret_key_here_change_this

# IMPORTANT SECURITY NOTES:
# 1. Never commit the actual .env file to version control
# 2. Use different secrets for development and production
# 3. Keep JWT_SECRET at least 32 characters long
# 4. Each developer should generate their own JWT_SECRET for local dev